<?php 

// utilizados para operações matemáticas

//soma (+), subtração (-), multiplicação (*), divisão (/), módulo (%)

// div (/) retorna o quociente da divisão e mod(%) retorna o resto

$a = 10;
$b = 5;
$soma = $a + $b;  // Resultado: 15
$divisao = $a / $b;  // Resultado: 2

// operadores de atribuição: igual (=), Atribuição com operação (+=, -=, *=, etc.)

// operadores de comparação: Igualdade (==), Identidade (===), Diferente (!=), Maior que (>), Menor que (<)

// operadores lógicos: AND (&&), OR (||), NOT (!)

?>
